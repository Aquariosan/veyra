{
  "name": "veyra",
  "private": true,
  "version": "0.0.1",
  "description": "Hosted trust and settlement layer for production AI agent actions",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @veyra/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
